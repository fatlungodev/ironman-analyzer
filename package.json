{
  "name": "ironman-analyzer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4273 --strictPort",
    "deploy:local": "npm run build && npm run preview"
  },
  "devDependencies": {
    "vite": "^6.4.1"
  }
}
